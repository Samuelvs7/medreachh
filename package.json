{
  "name": "medreach",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-modules --es-module-specifier-resolution=node server.js",
    "dev": "nodemon --experimental-modules --es-module-specifier-resolution=node server.js",
    "inject:firebase": "node scripts/inject-firebase-config.js",
    "prestart": "npm run inject:firebase",
    "predev": "npm run inject:firebase",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "firebase": "^12.0.0",
    "firebase-admin": "^13.4.0",
    "mongodb": "^6.18.0",
    "mongoose": "^8.16.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
